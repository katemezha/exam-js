import { SearchDirectionPipe } from './search-direction.pipe';

describe('SearchDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
